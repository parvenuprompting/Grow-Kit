# Plan 2026-09-10 — Zero_Trust_Bot

**Bron:** plannen/2026-09-09.md (gisteren), Agent-Family-Brain
(projecten/growkit.md + monetisatie-roadmap 9 sept, goedgekeurd),
docs/plannen/skills-telegram-lokaalmodel-bouwplan.md.

**Repo-staat bij start:** branch `zt/slices-zt3-zt4` heeft ZT-3
(dynamische hero) al committed; ZT-4 (tip-rotor) is in de lucht —
`tests/test_tips_rotor.py` bestaat (rood, module ontbreekt), 
`kern/growkit_tips.py` en de tips-data nog niet. Vandaag eerst die
open eind afmaken, daarna één nieuwe slice. Maximale slices: 2.

---

## ZT-5 — Tip-rotor afronden (was ZT-4, 9 sept — in de lucht)

- **Titel:** tip-rotor: kern + tips.json + SwiftUI-wiring.
- **Doel:** de al aanwezige (rode) `tests/test_tips_rotor.py` groen
  maken. Kiezen: `kern/growkit_tips.py` (conditie-tips vóór generieke,
  kans 0 bij laatst-getoond vandaag, geen tips na 23:00, geen tip binnen
  60 s bij gebeurtenis-hero, [N]-placeholders uit tellers, max 3
  "volgende"-wissels per sessie) + `app/Resources/tips.json` (12 tips,
  vast formaat id/tekst/scherm/knop/conditie) + wiring in HomeView als
  build-bewijs.
- **Failing test eerst:** bestaat al — `tests/test_tips_rotor.py`
  (6 gevallen, nu rood omdat de module ontbreekt). Ik maak geen nieuwe
  test; ik laat de bestaande eis staan en bouw de module ernaar.
- **Status:** todo

## ZT-6 — Skills-beheer fase A1: kern (TDD)

- **Titel:** `kern/growkit_skills.py` — skills lezen, schrijven,
  valideren.
- **Doel:** eerst de kern, geen scherm. `lijst_bron()` (paden Mac-lokaal
  + VPS-profiel), `lees(bron, naam)`, `schrijf(bron, naam, inhoud)`
  met verplichte backup `SKILL.backup-<ts>.md` vóór overschrijven
  (append-only geest), `valideer(inhoud)` (frontmatter + minstens één
  kop + secrets-scanner die keys weigert). Adapter-commando's
  `skillslijst`, `skillslees`, `skillsschrijf`.
- **Failing test eerst:** `tests/test_skills_kern.py` —
  1. lijst_bron geeft beide bronnen terug (gedirigeerde tmp-root);
  2. lees retourneert exacte SKILL.md-inhoud;
  3. schrijf maakt eerst een backup met timestamp, overschrijft pas
     daarna (append-only);
  4. valideer: zonder frontmatter → fout; zonder kop → fout;
  5. valideer: inhoud met een API-key-patroon → geweigerd, geen crash.
  (AI-herschrijffase A2/scherm komt in een latere slice — vandaag geen
  chat-pijplijn.)
- **Status:** todo

---

**Niet vandaag (bewust):** fase A2-scherm (Skills-beheer UI), Telegram
wizard B1/B2 (al deels aanwezig als TelegramWizardView — eerst A-keten
afronden), monetisatie-fase 0 (sponsorware-bron aan de Baas, niet een
code-slice). Zoals de skills-wet zegt: alles na 23:00 geen tips, alles
main-merges wacht op goedkeuring.

**Repo/branch-aangeraakt:** parvenuprompting/Grow-Kit, branch
`zt/plans-2026-09-10` (alleen deze plan-file).