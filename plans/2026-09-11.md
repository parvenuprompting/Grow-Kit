# Plan 2026-09-11 — Genius

**Bron:** brein `projecten/growkit.md` (ThreatLocker-nabouw, besluit 5 sept),
familie-huis-slice-rij (F = Genius-voorstellen als kaarten), repo-scan van
`kern/` (11 sept: geen beleids-module, geen voorstellen-module, PTS en
TestLock bestaan al van KairOS). Ook aangeleverd in de brein-inbox.
Maximale slices: 3.

---

## GEN-1 — Beleids-laag per agent (ThreatLocker-nabouw, stap 1, TDD)

- **Titel:** `kern/growkit_beleid.py` — per agent allow/deny op actieklassen.
- **Doel:** de politiek-laag uit het ThreatLocker-nabouw: per agent een
  beleidsbestand met allow/deny op de actieklassen die
  `growkit_goedkeuring.py` al classificeert (lezen/schrijven/wissen/git/
  netwerk/secrets/systeem). `mag(agent, actieklasse)` geeft toegestaan of
  geblokkeerd, met uitleg in simpele taal. Adapter-commando `beleid`.
- **Eerste failing test:** `tests/test_beleid.py` —
  1. agent zonder beleid = alles toegestaan, wel gelogd;
  2. deny op `secrets` + aanroep op secrets-actie → blok met uitleg;
  3. onbekende actieklasse = blok (fail-closed op onbekend).
- **status: todo**

## GEN-2 — Voorstellen-kaarten: plans/*.md leesbaar in de app (TDD)

- **Titel:** `kern/growkit_voorstellen.py` — slice-plannen parseren tot kaarten.
- **Doel:** de familie-huis-slice F: dagelijkse slice-voorstellen van agents
  (plans/*.md in plans-branches) worden kaart-objecten (id, titel, doel,
  eerste failing test, status) die de app kan tonen. Adapter-commando
  `voorstellen`; SwiftUI-kaarten in een vervolgslice.
- **Eerste failing test:** `tests/test_voorstellen.py` — parse van
  `plans/2026-09-11.md` levert ≥ 2 kaarten met id+titel+status; slice-blok
  zonder kop geeft een waarschuwing, geen crash.
- **status: todo**

## GEN-3 — Lessen Geleerd ronde 2 publiceren op grow-kit-site

- **Titel:** 8 nieuwe lessen uit Drive live zetten (ronde 2).
- **Doel:** de site toont 10 lessen; ronde 2 ligt klaar in Drive.
  Publicatieronde met het bestaande les-sjabloon en bron-labels
  ([Claude]/[Familie]/[Familie-fout]). Eén push = deploy ±20 s.
- **Eerste stap:** lessen ophalen uit de Drive-map, checklist (titel,
  bron-label, afbeeldingen), per les publiceren en met curl verifiëren dat
  de pagina live is.
- **status: todo**